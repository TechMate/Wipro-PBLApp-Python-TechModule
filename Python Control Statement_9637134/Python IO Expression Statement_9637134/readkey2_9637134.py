num1_ADID = int(input("Enter the value as 100"))
num2_ADID = int(input("Enter the value as 200"))
sum1_ADID = num1_ADID + num2_ADID
print(sum1_ADID)

"""#output : 300 here we have type casted the variables while taking the input from the console
into the integer type this has to do because anything pass as command line argument it will be taken as string and if we perfor any add operation on it 
 than it concatenate them insted of adding them"""